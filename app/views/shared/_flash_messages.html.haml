- messages.each do |key|
  - if flash[key]
    %span{:class => "message"}
      %span{:class => "#{key}"}
        = flash[key]
        = link_to_remote '[x]', :url => {:controller => 'application', :action => :close}, :update => "flash-#{key}", :html => {:class => 'link'}

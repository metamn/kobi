#index_expenses
  %ul.header
    %li.date
      = t('activerecord.attributes.expense.date')
    %li.amount
      = t('activerecord.attributes.expense.amount')
    %li.category
      = t('activerecord.attributes.expense.category')
    %li.tags
      = t('activerecord.attributes.expense.tags')
  %ul.items  
    - @expenses.each do |expense|
      .item{:class => "#{cycle('even', 'odd')} block"}
        %li.date
          = expense.date
        %li.amount
          = expense.amount
        %li.category
          = expense.category.name unless expense.category.nil?
        %li.tags
          = expense.tag_list
        %li.buttons
          = link_to_remote 'Mod', :url => edit_expense_path(expense), :method=> :get, :update => "ajax-update-#{expense.id}"
          = link_to 'Sterge', expense, :confirm => 'Are you sure?', :method => :delete
        %div{:id => "ajax-update-#{expense.id}"}
      
